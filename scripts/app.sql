!-- SQL -->
<!--
  SQL (Structured Query Language) is used to interact with databases. It allows for data retrieval, insertion, updating, and deletion. 
  SQL is typically used server-side to query relational databases.
-->
<!--
  Example SQL query:
  SELECT * FROM users WHERE age > 18;
  
  In a web application, SQL queries are executed on the server and results are sent to the client as JSON or another format.
-->
